"""Simple backtest engine — stub."""

from __future__ import annotations
import pandas as pd


def run_backtest(weights: pd.DataFrame, tri: pd.DataFrame, costs_bps: float = 0.0) -> pd.DataFrame:
    """Return equity curve and ledger (stub)."""
    raise NotImplementedError
